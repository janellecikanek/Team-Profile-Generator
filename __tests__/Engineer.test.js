// In addition to Employee's properties and methods, Engineer will also have:
// github // GitHub username - can set Github account via constructor agrument
// getGithub()  can get github username via getGithub()
// getRole() // Overridden to return 'Engineer' 